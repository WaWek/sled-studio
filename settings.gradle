$lines = @(
    'pluginManagement {',
    '    repositories {',
    '        google()',
    '        mavenCentral()',
    '        gradlePluginPortal()',
    '    }',
    '}',
    '',
    'dependencyResolutionManagement {',
    '    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)',
    '    repositories {',
    '        google()',
    '        mavenCentral()',
    '    }',
    '}',
    '',
    'rootProject.name = "SLED-Studio"',
    'include ":app"'
)
[IO.File]::WriteAllLines("C:\sled-studio\settings.gradle", $lines, (New-Object System.Text.UTF8Encoding($false)))

Write-Host ""
Write-Host "=== settings.gradle ==="
Get-Content C:\sled-studio\settings.gradle